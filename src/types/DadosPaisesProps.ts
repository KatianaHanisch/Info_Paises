export type DadosPaisesProps = {
  flags: {
    png: string;
  };
  name: {
    common: string;
  };
  population: number;
  capital: string;
  continents: string;
  currencies: string;
  area: number;
  languages: string;
};
